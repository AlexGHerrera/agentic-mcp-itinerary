[build]
builder = "RAILPACK"

[deploy]
startCommand = "PYTHONPATH=server python server/main.py"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
